---
- name: Create namespace in OpenShift
  hosts: localhost
  gather_facts: no

  tasks:
    - name: Create namespace
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: v1
          kind: Namespace
          metadata:
            name: "{{ namespace }}"
